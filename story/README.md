# Story files moved

The editable book content now lives under **`versions/`**:

- **v1** (finished gift): `versions/v1/story.json`
- **v2** (redesign WIP): `versions/v2/story.json`

The file `story/story.json` in this folder is kept as a legacy copy. Edit the version you are working on under `versions/{v}/story.json`.

See `versions/README.md` for full details.
